let shedule = {}

shedule["8:30"] = "get up";

function isEmpty(n){
  for (const key in n) {
    if(key === undefined){   }
    else{    }
  }
}
//isEmpty(shedule)

let menu = {
  width: 200,
  height: 300,
  title: "My menu"
};

for (const element of menu) {
  console.log(element);
}

